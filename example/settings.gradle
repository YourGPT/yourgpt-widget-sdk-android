pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google()
        mavenCentral()
        maven { url 'https://jitpack.io' }
    }
}

rootProject.name = "YourGPT Android Example"

// For local SDK development, uncomment the lines below and swap the
// dependency in build.gradle to: implementation project(':yourgpt-sdk')
// include ':yourgpt-sdk'
// project(':yourgpt-sdk').projectDir = new File(settingsDir, '../')