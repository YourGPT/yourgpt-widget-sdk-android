<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme -->
    <style name="Theme.YourGPTExample" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/accent_primary</item>
        <item name="colorPrimaryDark">@color/accent_primary</item>
        <item name="colorAccent">@color/accent_secondary</item>
    </style>

    <!-- NoActionBar variant for HomeScreenActivity -->
    <style name="Theme.YourGPTExample.NoActionBar" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/accent_primary</item>
        <item name="colorPrimaryDark">@color/accent_primary</item>
        <item name="colorAccent">@color/accent_secondary</item>
    </style>

    <!-- Splash screen theme for launcher activity -->
    <style name="Theme.YourGPTExample.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/background_primary</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/accent_primary</item>
        <item name="postSplashScreenTheme">@style/Theme.YourGPTExample</item>
    </style>
</resources>
